#include <stdio.h>

/*
Challenge:

Write a program that takes an integer input representing a month (1 to 12) and prints the name of the month. If the input is not between 1 and 12, print "Invalid month."

Requirements:

    Use a switch statement to handle each month.
    Handle invalid inputs.
*/